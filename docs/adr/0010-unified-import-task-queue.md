# 瀏覽器與桌面匯入共用任務隊列

瀏覽器選檔與 Windows 右鍵匯入統一建立後端匯入任務，由同一隊列管理等待、分析、成功與失敗狀態，並在兩個入口之間合計最多同時分析三份文檔。集中管理可讓瀏覽器在桌面文件分析期間顯示一致的任務狀態，並確保完成動畫能從對應文件任務項目發出；代價是瀏覽器匯入不再由前端單獨掌握完整生命週期。

隊列僅存在於後端進程記憶體，不承諾在服務重啟後恢復等待中或分析中的任務；此限制符合目前本機 demo 的資料邊界，並避免引入額外持久化系統。
